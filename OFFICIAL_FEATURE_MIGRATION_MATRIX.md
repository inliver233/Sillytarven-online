# Official Feature Migration Traceability Matrix

## Verified Baseline

- Target baseline: `320795b4fc88c2faf410b7f1883eb0e7b686910c` (`origin/main` at fetch time).
- Development branch/worktree: `dev` in `Sillytarven-online-dev`.
- Official boundaries verified from tags:
  - `1.15.0`: `1c0e7ea9556afcc3ae55c888287dff67a6aa42fc`
  - `1.16.0`: `e3b866b5d2bcc7fbaa889bb926fbb567cd1ed25b`
  - `1.17.0`: `e3f41666c69db032e17e079fcddcf40cf47e8593`
  - `1.18.0`: `51ad27fb86d39a3daca3adaa970375c9670c12df`
- Official range counts verified with `git log`: 148 commits in `(1.15,1.16]`, 136 in `(1.16,1.17]`, 107 in `(1.17,1.18]`.
- Evidence method: `git show`, `git diff-tree`, `git cat-file`, and `git grep <tree>` against official Git objects. The dirty official worktree was not used or modified.
- Status values: `planned` means source evidence is verified but implementation has not yet been committed. Rows are updated with tests and commit/push hashes as work lands.

## Cross-Cutting Storage Preconditions

| Feature point | Official/source evidence | Target state at baseline | Conflict/risk | Adaptation | Tests | Commit/push |
|---|---|---|---|---|---|---|
| Durable chat transaction | Target `src/endpoints/chats.js`: `createChatWriteSnapshot`, `writeChunkedChat`, `truncateChunkedChat`, `appendChunkedMessages` | Version-2 journal has durable `prepared`, `mutating`, and `committed` states; router recovery is scoped to the authenticated user | Crash can expose partial shard/index/header/metadata/revision state | Journal namespace is `path.join(path.dirname(userRoot), '.migration-journals', sha256(handle))`; manifests store only user-root-relative targets and enforce root/handle fingerprints, artifact families, regular files, and no symlinks | Real child-process crashes cover `mutating` rollback and committed-marker cleanup; prepared recovery, malicious target/artifact manifests, symlink rejection, and A/B router isolation are also covered | `b98444c` (pushed `origin/dev`) |
| Chat artifact contract | Target constants and `listChatArtifactPaths()` | Main `.jsonl`, `.metadata.json`, `.index.json`, `.revision.json`, and `.chunks/` are snapshotted as one unit, including empty chunk-directory presence | Specs omitted `.revision.json` | Treat all five as one unit; preserve legacy readers | Five-part child-process recovery plus canonical legacy/chunk-like manifests at counts 0/1/199/200/299/300/301/1000 | `b98444c` (pushed `origin/dev`) |
| Canonical snapshot/hash | No complete official equivalent; required by migration specification | Canonical JSON, SHA-256, deterministic file manifests, verification, and in-memory dry-run snapshot/diff APIs are implemented | Unknown JSON fields or byte-stable message lines may be lost silently | Recursive key ordering for JSON; byte hashes for artifacts; pure dry-run diff reports JSON, file, and directory changes | `canonical-hash.node.test.mjs` and `canonical-snapshot-fixtures.node.test.mjs` verify unknown fields, canonical before/after values, raw legacy message bytes, and unchanged shard/index hashes at all eight required counts | `b98444c` (pushed `origin/dev`) |
| Feature gates | Requested migration contract; no single official commit | Five additive migration gates default false | Partial rollout cannot be isolated | Public boolean projection for `macros2`, `reasoningTools`, `extensionLifecycle`, `swipePicker`, and `worldInfoRelink` | Default config, one-at-a-time isolation, type, and public endpoint tests | `b98444c` (pushed `origin/dev`) |
| Reliable browser test target | Target `tests/playwright.config.js` and `frontent-test-utils.js` previously hard-coded port 8000 | Both now resolve `ST_TEST_URL` with the existing URL as fallback | Tests can hit old code and time out selecting one account | Environment-driven base URL and account/discreet-login helper; isolated targets serialize workers | URL projection tests plus full Macros browser conformance on `http://127.0.0.1:8011` | `b98444c` foundation; `564f317` auth hardening (both pushed) |

## Macros 2.0

`M` below means `public/scripts/macros`; `A` means `public/scripts/autocomplete`.

Stage result: implemented in `564f317` and pushed to `origin/dev`. Browser conformance executed against the isolated dev server: `328/328` MacroEngine, `178` remaining official tests passed, `13` upstream-deferred output-filter tests skipped by official `355598bb`, and `5/5` cloud integration tests passed. Focused source/test lint had zero findings; Node migration/feature tests were `42/42`; optimization, import, registration, and invitation-enabled regressions were `91/91`, `34/34`, `14/14`, and `10/10`.

| Official commit / verified boundary | Official actual diff and behavior | Target state / conflict | Adaptation | Tests | Commit/push |
|---|---|---|---|---|---|
| `e9bedadc` `(1.15,1.16]` | CSS, autocomplete bases, core definitions, `MacroFlags`, lexer/parser/CST/engine/registry, power-user, slash parser, 5 test files; scoped macros, if/else, trim/dedent | Core files equal parent snapshot; `MacroFlags` absent | Port subsystem files; merge only integration hunks | Official engine/lexer/parser/registry/story tests | `564f317` (pushed) |
| `0dcd9906` `(1.15,1.16]` | `.local`/`$global`, conditional variables; CST/flags/lexer/parser/autocomplete/tests | Missing | Port final semantics | Variable isolation and condition tests | `564f317` (pushed) |
| `dbc4fe61` `(1.15,1.16]` | Case-insensitive registry, aliases, dynamic/scoped closing | Missing | Port final engine/registry | Case and alias tests | `564f317` (pushed) |
| `81414724` `(1.15,1.16]` | Delayed argument resolution and lazy branch handler `resolve()` | Missing | Port; preserve no-side-effect unselected branches | Lazy nested if tests | `564f317` (pushed) |
| `b453fdc5` `(1.15,1.16]` | Alias display, closing whitespace, move `M/MacroBrowser.js` to `M/engine` | Old path/import remains | Move and update imports without replacing cloud callers | Import/browser tests | `564f317` (pushed) |
| `7331dba0` `(1.15,1.16]` | Dynamic definitions; STscript closure/parser integration; `script.js` | Missing; `script.js` high conflict | Port engine and closure hunks only | Slash closure/dynamic tests | `564f317` (pushed) |
| `3047045d` `(1.15,1.16]` | Reparse scoped content/arguments with global offsets | Missing | Port final CST/engine | Nested scoped argument tests | `564f317` (pushed) |
| `5c2a02a1` `(1.15,1.16]` | Delayed pick inherits global offset | Missing | Port final engine/registry | Deterministic delayed pick tests | `564f317` (pushed) |
| `f8c373f5` `(1.15,1.16]` | Operators, lazy fallback, has/delete variables; `st-context`/`variables` API | Missing | Port and expose `has` without changing user scope keys | Local/global multi-user tests | `564f317` (pushed) |
| `0b529290` `(1.15,1.16]` | Runtime warning context | Missing | Port | Warning argument tests | `564f317` (pushed) |
| `42155ece` `(1.15,1.16]` | Numeric comparison and autocomplete | Missing | Port | Operator tests | `564f317` (pushed) |
| `ca60ba14` `(1.15,1.16]` | Independent alias registration/public API | Missing | Port | Alias-of-alias tests | `564f317` (pushed) |
| `06b77ec9` `(1.15,1.16]` | Autocomplete cursor/operator/value boundaries | Helper absent | Port final autocomplete | Browser autocomplete tests | `564f317` (pushed) |
| `9f444997` `(1.15,1.16]` | Optional scope, nested closing completion | Missing | Port final autocomplete/CST | Scope completion tests | `564f317` (pushed) |
| `953d9f34` `(1.15,1.16]` | List min/max validation and completion | Missing | Port | Arity/list tests | `564f317` (pushed) |
| `cd0627bf` `(1.15,1.16]` | Shared `findExtension()` and `hasExtension` | Missing; cloud local/global shadow exists | Use canonical resolver from lifecycle work | Resolver and macro tests | `564f317` (pushed) |
| `e40b31b0` `(1.15,1.16]` | Detect new syntax and offer engine opt-in | Missing | Merge diagnostics/UI only | Legacy opt-in test | `564f317` (pushed) |
| `281c50bb` `(1.15,1.16]` | Initialize macros before extensions | Target initializes extensions first | Reorder while preserving preload and async startup | Initialization order test | `564f317` (pushed) |
| `c705cbe6` `(1.15,1.16]` | Removes duplicate env `summary` registration | Target still has duplicate env + memory registration | Remove duplicate after lifecycle order is deterministic | Memory summary precedence test | `564f317` (pushed) |
| `61891853` `(1.15,1.16]` | Reload prompt after engine toggle | Missing | Port without rewriting saved value | Existing-user setting preservation | `564f317` (pushed) |
| `26d495f4` `(1.15,1.16]` | `/reroll-pick`, optional `pick_reroll_seed` | Missing | Add optional field only | Seed/reroll tests | `564f317` (pushed) |
| `9ff9d596` `(1.15,1.16]` | Global `data-macros`, observer and autocomplete modules | Missing; HTML/script/style high conflict | Attribute/function-level integration | Input/autocomplete E2E | `564f317` (pushed) |
| `bee4d9a8` `(1.15,1.16]` | Avoid pristine greeting swipe overwrite | Missing; paged chat state differs | Merge into cloud swipe save path | Legacy greeting macro test | `564f317` (pushed) |
| `eeda4d37` `(1.16,1.17]` | Context/response/prompt token APIs/macros | Missing | Preserve prompt dry-run scheduler | Max-token tests | `564f317` (pushed) |
| `b5a1d227` `(1.16,1.17]` | Lazy greeting/alternate greeting macros | Missing | Merge logical-message lookup for paging | Greeting index tests | `564f317` (pushed) |
| `f5b1f913` `(1.16,1.17]` | New install default enables engine | Target default false | Preserve existing values; gate/admin template policy for new users | Old/new settings fixtures | `564f317` (pushed) |
| `8879272a` `(1.16,1.17]` | Summary prefers current memory UI | Missing | Port into lifecycle-exported init | Summary tests | `564f317` (pushed) |
| `ff4c6fa1` `(1.17,1.18]` | Legacy macro warnings include call arguments | Missing | Port | Warning test | `564f317` (pushed) |
| `aec9754b` `>1.18 staging` | Scoped comment closing and six tests | Missing | Backport verified fix | Six comment regressions | `564f317` (pushed) |
| `355598bb` `>1.18 staging` | Disables pipe token and skips output-filter suite; it does not implement pipe parsing | Target has active incomplete pipe token | Preserve literal `|` argument behavior; do not claim filters | Literal/escaped pipe tests; skipped filter capability documented | `564f317` (pushed) |
| `93bd9e21` `>1.18 staging` | Variable key/index APIs | Missing | Add only if variable API supports keyed access safely | Key/index tests | `564f317` (pushed) |
| `2463e839` `>1.18 staging` | Source whitespace-only change | No behavior to migrate | Excluded | N/A | verified exclusion |
| `8372e7bf` `(1.15,1.16]` | 44-file mechanical dot-notation refactor plus unrelated guards | Not a semantic dependency | Excluded from migration | N/A | verified exclusion |

## Reasoning, Tool Calling, Streaming Errors

`N` below means `tests/reasoning-tools.node.test.mjs`; `R` means `tests/stage2-protocol-integration.node.test.mjs`; `P` means `tests/frontend/ReasoningTools.e2e.js`.

Stage result: all 19 listed official commit objects were re-verified with `git cat-file`/`git show`, implemented in commit `7e4e8f601bd28754374f17be7aff6412d679feb1`, and pushed to `origin/dev`. Final focused verification on Node 22.13.1: `N` 19/19, `R` 7/7, `P` 7/7 serial plus explicit mobile 1/1, all with zero failures and zero skips. Regression gates: phase 0 34/34, optimizations 91/91, imports 34/34, registration 14/14, invitations 10/10 under isolated enabled config, and focused production/test ESLint with zero warnings. The canonical chat/group manifest remains unchanged at `a5cbbf98118fad9daab66102fa191e5716007227285f52acf778759d02197643`.

Cloud-wide adaptation: `default/config.yaml`, `src/endpoints/public-config.js`, and `public/scripts/reasoning-tools/feature-gate.js` add an off-by-default `reasoningTools` gate, public gate/cap projection, `loadReasoningToolsFeatureGate()`, `isReasoningToolsEnabled()`, `getToolRecurseHardLimit()`, and non-mutating `normalizeRecurseLimit()`. The gate controls the new UI, modern recurse-limit selection, reasoning forwarding, and OpenRouter request shape while retaining the legacy recurse limit/request shape when disabled. Correctness and isolation fixes remain unconditional: `Generate()` always preserves input at recursive depth, group-member reasoning/signatures never cross characters, and `forwardFetchResponse()` settles on end/error/premature upstream close or downstream disconnect.

| Official commit / boundary | Official actual diff and behavior | Target evidence / conflict | Cloud adaptation | Tests | Commit/push |
|---|---|---|---|---|---|
| `06691e8b` `(1.15,1.16]` | Gemini thought-signature config/converter | Implemented in `default/config.yaml` and `src/prompt-converters.js`; cloud already persisted signatures and could leak them across provider/model transitions | `gemini.thoughtSignatures: true`; `enableThoughtSignatures` guards both `convertGooglePrompt()` and `addOpenRouterSignatures()`, while `setOpenAIMessages()` removes incompatible signature/reasoning metadata | `N`: defaults plus converter/signature contract; `19/19` stage suite passed | `7e4e8f6` (pushed) |
| `97143747` `(1.15,1.16]` | Preserve original user input during tool recursion | Implemented in `public/script.js:Generate()`; recursive calls previously re-entered command/input/send/delete paths in the cloud generation flow | `depth` suppresses command processing, textarea clearing, user resend, previous-message deletion, attachments, and `send_if_empty`; both recursive branches forward `jsonSchema` | `N`: recursive input/schema guards; `R`: CUSTOM normal/stream schema; both passed | `7e4e8f6` (pushed) |
| `0ba0418f` `(1.16,1.17]` | Three tool reasoning modes across UI/script/openai/reasoning/tools/locales | Implemented across `public/index.html`, `public/script.js`, `public/scripts/openai.js`, `reasoning.js`, `tool-calling.js`, and 16 locale files; cloud settings and token budgeting required function-level integration | Adds disabled/since-last-user/active-chain modes; `populateChatHistory()` rebuilds invocation reasoning without mutating saved history, `Message.setToolCalls()` forwards eligible plaintext reasoning, and invocation save captures editable reasoning; exposed only for OpenRouter/custom under the gate | `N`: mode/provider, invocation, locale, and isolation contracts; `P`: controls, persistence, mobile layout, protocol, multipart; all passed | `7e4e8f6` (pushed) |
| `0cef10f6` `(1.16,1.17]` | Explicit OpenRouter reasoning exclusion when hidden | Implemented in `getReasoningEffort()` and `src/endpoints/backends/chat-completions.js`; replacing the cloud request shape unconditionally would change disabled-gate behavior | Gated path sends `reasoning: { exclude, effort? }`; disabled gate preserves legacy `include_reasoning` and legacy effort object; minimum effort plus hidden thoughts maps to `none` | `N`: legacy and gated OpenRouter request-body assertions passed; `R`: route payload coverage passed | `7e4e8f6` (pushed) |
| `15e2f240` `(1.16,1.17]` | Cache-at-depth skips system message | Implemented in `cachingAtDepthForOpenRouterClaude()`; cloud can contain a hoisted system entry amid cached chat history | Skip system entries without consuming alternating-role cache depth | `N`: hoisted-system cache-depth behavior and exact cache-control target passed | `7e4e8f6` (pushed) |
| `63fa9c1d`,`3070cf26`,`b259c975` `(1.16,1.17]` | Claude adaptive mapping/config/final default false | Implemented in `default/config.yaml`, `calculateClaudeBudgetTokens()`, and `sendClaudeRequest()`; cloud must preserve old missing-key behavior while new installs default off | New config defaults false, backend lookup falls back true for an absent old key; adaptive effort strings, fixed budgets, multipart thinking, no-prefill/no-sampling models, and summarized display are selected per model | `N`: adaptive/fixed budget and final model semantics passed; `R`: Opus 4.7 multipart request shape passed | `7e4e8f6` (pushed) |
| `9bce79f7` `(1.16,1.17]` | Concatenate non-stream multipart text/thinking | Implemented in `extractMessageFromData()` and `extractReasoningFromData()`; first-part selection lost later cloud response blocks | Filter/map/join all text and thinking blocks with double newlines while leaving tool blocks intact | `N` and `P`: multipart text/reasoning cases passed | `7e4e8f6` (pushed) |
| `c3f36b2b` `(1.16,1.17]` | OpenRouter signature respects config | Implemented in `addOpenRouterSignatures()` plus `setOpenAIMessages()` cleanup; cloud supports account/group/provider switching | Honor Gemini signature config and clone/remove invocation signature and reasoning when API/model changes, avoiding mutation of persisted history | `N`: provider/signature cleanup contract passed | `7e4e8f6` (pushed) |
| `94139f46` `(1.16,1.17]` | Lightweight intermediary finalization | Implemented by `StreamingProcessor.finalizeIntermediaryMessage()`; cloud `StreamRenderBuffer` can retain a pending rAF callback and paged tail saves must not observe a stale intermediary state | Cancel `streamRenderBuffer` before deleting a tool-only message; otherwise finalize reasoning/signature/swipe/media/events before tool invocation with `unlockUI:false`, then leave final save/unblock to the existing cloud flow | `N`: cancellation/finalization ordering; `P`: real buffer coalescing/cancel; both passed | `7e4e8f6` (pushed) |
| `b8929341` `(1.17,1.18]` | Preserve `Error` and write `error:true` invocation | Implemented in `ToolManager.invokeFunctionTool()`/`invokeFunctionTools()`; dropping failures makes recursion/provider behavior unstable | Return `Error` objects; persist non-stealth failures with string result, `error:true`, signature, and reasoning; retain errors list; stealth failures remain terminal and unpersisted | `N` and `P`: success/throw/empty/stealth/error-invocation protocol passed | `7e4e8f6` (pushed) |
| `c9c652ee` `(1.17,1.18]` | Awaitable non-2xx response forwarding in seven backend/middleware files and util tests | Implemented in async `src/util.js:forwardFetchResponse()` and all 19 cloud callers across six caller files; target already had improved close handling | Preserve 403 JSON/502 text bodies and 401-to-400 mapping; await forwarding, keep one-shot close cleanup, destroy upstream on client close, omit unsafe/entity headers, and settle only after stream end/close/error | `N`: 403, 502, 401, SSE, abort, unsafe-header and all-19-caller cases; `R`: route errors, client abort and premature upstream close; all passed | `7e4e8f6` (pushed) |
| `f4f390f3` `(1.17,1.18]` | Custom stream state includes images/signatures/tool signatures | Implemented in the `ChatCompletionService` streaming `streamData()` state; singular cloud `image` state was incomplete | Initialize `{ reasoning, images, signature, toolSignatures }` to match `getStreamingReply()` consumers | `N`: custom stream-state/signature contract; `P`: streaming signature state; both passed | `7e4e8f6` (pushed) |
| `051346c5` `(1.17,1.18]` | Custom OpenAI interleaved reasoning | Implemented in `interleaved_reasoning_providers` and custom controls; provider transitions must not reuse incompatible metadata | Add custom provider without broadening unrelated providers; reuse gated mode selection and provider/model cleanup | `N`: custom-provider and isolation assertions; `P`: custom real-control scenario; both passed | `7e4e8f6` (pushed) |
| `552936a0` `(1.17,1.18]` | Filter other group characters' reasoning/signatures | Implemented in `Generate()` prompt reasoning and `setOpenAIMessages()`; cloud groups expose only a paged suffix and must not full-load history | Filter by current `name2`, stripping message and invocation reasoning/signatures for other members; operate on the existing loaded page, and keep `generateGroupWrapper()` on `Generate()` without `/api/chats/group/get` or a full-history reload | `N`: group/provider isolation and no-full-load paging assertions passed | `7e4e8f6` (pushed) |
| `7948886c` `(1.17,1.18]` | Configurable recurse limit, default 5 | Implemented in `ToolManager.configureRecurseLimit()`/`getRecurseLimit()`, UI, public config route, and feature-gate module; official user-only slider is insufficient for cloud policy | Validate user/default/hard-cap values in `1..50`, use `min(user, instance)` only under the gate, preserve legacy fixed 5 when disabled, and never rewrite saved settings during gate/config load | `N`: defaults/bounds/effective-min/no-rewrite; `P`: sentinel persistence, mobile controls, effective cap; both passed | `7e4e8f6` (pushed) |
| `940b3722` `(1.17,1.18]` | Empty/pure-tool cache tail guard | Implemented in `cachingAtDepthForOpenRouterClaude()` alongside the system skip | Apply cache control only when multipart content has an element; empty and pure-tool tails no longer index `-1` | `N`: empty/pure-tool tail safety plus exact eligible cache-control target passed | `7e4e8f6` (pushed) |
| `338e35fc` `(1.17,1.18]` | Custom JSON Schema normal/stream/quiet/group | Implemented through `generateQuietPrompt()`, `generateRaw()`, `Generate()`, `sendGenerationRequest()`, `sendStreamingRequest()`, `createGenerationParameters()`, and custom backend `response_format`; cloud recursion and paging added extra paths | Forward the same `jsonSchema` through normal, streaming, quiet, raw, paged group wrapper, and both recursive tool branches; CUSTOM translates it to strict `response_format.json_schema` without replacing provider-specific paths | `N`: normal/stream/quiet/raw/group/both-recursion matrix; `R`: actual CUSTOM normal/stream translation; all passed | `7e4e8f6` (pushed) |

## Extension Manifest Lifecycle

Stage 3 source state: fully implemented/adapted in the current dirty worktree and not committed or pushed; status remains uncommitted until the main implementation is committed. The nine official objects below were re-verified from the sibling official object store with `git show`. Final verification passed focused Node `66/66`; browser E2E `4/4` plus explicit mobile `1/1`; imports `34/34`, optimizations `92/92`, registration `14/14`, and invitations `10/10`; and focused production/test ESLint with zero errors and zero warnings. All 14 builtins are migrated. The canonical chat/group manifest remains unchanged at `a5cbbf98118fad9daab66102fa191e5716007227285f52acf778759d02197643`, and the configuration hash remains unchanged at `b7fa...`.

Cloud adaptation: `public/scripts/util/extension-resolver.js:createExtensionDescriptors()`/`ExtensionResolver.resolve()` and `src/endpoints/extensions.js:getCanonicalExtensionIdentity()`/`resolveTypedExtension()` retain canonical and typed builtin/local/global identity, accent/case normalization, unambiguous short aliases, and deterministic local shadowing. The lifecycle implements install, enable, disable, update, clean, and delete hooks with serialized typed operations, explicit results, cooperative timeout cancellation, and clean-before-delete sequencing. `resolveExtensionResource()` plus `createExtensionResourceRouteHandler()` select the current user's local root before global, forbid cross-root fallback/traversal/symlinks, and send `Vary: Cookie` with `Cache-Control: private, no-cache, must-revalidate`; `src/users.js` installs the authenticated route, and the pre-auth static bypass now routes encoded and normalized third-party paths to it. Server discovery, resources, and management enforce `extensions.enabled`; extension installation rejects literal or DNS-resolved private targets and pins a validated DNS address for the clone while redirects are disabled. The off-by-default public lifecycle gate is projected by `src/endpoints/public-config.js`, cached/fail-closed by `public/scripts/extensions/feature-gate.js:loadExtensionLifecycleFeatureGate()`, awaited with the other startup gates in `public/script.js`, and preserves legacy loading when disabled.

| Official commit / boundary | Official actual diff and behavior | Target evidence / conflict | Cloud adaptation | Tests / verification | Commit/push |
|---|---|---|---|---|---|
| `cd0627bf` `(1.15,1.16]` | Adds shared `findExtension()` lookup and uses it from extension slash commands and `{{hasExtension}}` | Implemented in dirty `public/scripts/util/extension-resolver.js` and consumed by `public/scripts/extensions.js:resolveExtensionDescriptor()`/`findExtension()`; exact canonical names win, aliases must be unambiguous | Descriptor contract is `{canonicalName,shortName,type,manifest,resourceBaseUrl,enabled}`; typed identities preserve local/global ownership and local shadows global without losing typed exact lookup | `tests/extension-resolver.node.test.mjs` covers contract, normalization, builtin/local/global shadowing, immutable enabled snapshots, and clone isolation; focused Node run passed | dirty worktree; uncommitted |
| `3ad9b05e` `(1.16,1.17]` | Adds manifest install/update/delete/enable/disable/activate hooks with a 5s timeout race | Fully adapted in dirty `public/scripts/util/extension-lifecycle.js` and integrated by `public/scripts/extensions.js`; install, enable, disable, update, clean, and delete hook paths are implemented and return explicit lifecycle state, hook status, and serialized errors | Imports use descriptor `resourceBaseUrl`, avoiding official short-name/local-shadow ambiguity; operations serialize and dedupe by typed identity and phase, timeout is exactly 5000ms with cooperative `AbortSignal`, inactive updates do not import, missing deactivation requires reload, and delete runs clean before server removal | Focused Node coverage includes sync/async/reject/import failure/missing export/invalid hook/timeout/late rejection/dedupe and all install/enable/disable/update/clean/delete sequences; final focused Node run passed `66/66` | dirty worktree; uncommitted |
| `f3521e70` `(1.17,1.18]` | Awaits extension initialization in `script.js` and migrates 14 builtin `index.js`/manifest pairs to activate hooks | Implemented across `public/script.js` and all 14 dirty builtin entrypoints/manifests; each manifest maps `hooks.activate` to exported `init` | Each builtin memoizes its initialization promise and retains gate-guarded legacy self-start only when lifecycle is disabled; startup awaits `initExtensions()` and lifecycle activation | `tests/builtin-lifecycle-batch-a.node.test.mjs` and `tests/builtin-extension-lifecycle-batch-b.node.test.mjs` cover all 14 manifests, guarded startup, one-time initialization, rejection memoization, and network/worker/registration placement; focused Node run passed | dirty worktree; uncommitted |
| `a7f144f2` `(1.17,1.18]` | Adds `getExtensionManifest()` and exposes it through extension context | Implemented in dirty `public/scripts/extensions.js:getExtensionManifest()` and `public/scripts/st-context.js:getContext()` | Gated path returns a structured-cloned manifest from the canonical resolver; lifecycle records and hook context also clone descriptors so caller mutation cannot alter resolver/lifecycle state | Clone/mutation isolation is covered in `tests/extension-resolver.node.test.mjs` and `tests/extension-lifecycle.node.test.mjs`; focused Node run passed | dirty worktree; uncommitted |
| `737cb95a` `(1.17,1.18]` | Adds optional `clean` hook, clean UI, and clean-before-delete behavior | Fully adapted: clean is exposed through the management flow, lifecycle status remains explicit, and delete invokes clean before server removal | Clean/delete uses the typed current-user/global-admin scoped descriptor; failed clean or removal preserves explicit failure state and cannot silently report deletion | Focused Node covers clean success/failure/timeout, clean-before-delete order, typed isolation, cancellation, and delete failure; browser E2E passed `4/4` plus explicit mobile `1/1` | dirty worktree; uncommitted |
| `5512473b` `(1.17,1.18]` | Adds extension source/URL/flags/install warning and management endpoint changes | Adapted without replacing the cloud management UI in dirty `public/scripts/extensions.js` and `src/endpoints/extensions.js:createExtensionsRouter()` | Existing `global` request shape remains compatible while explicit type is validated; builtins are unmanageable, global operations require admin, all routes enforce `extensions.enabled`, and normalized collisions are rejected before mutation; install accepts only HTTP(S), rejects literal and DNS-resolved private targets, disables redirects, and pins one validated DNS address for clone | Server-management tests cover feature disablement, management guards, SSRF/DNS pinning, legacy leading slash, install/move collisions, clone cleanup, delete failure, and move rollback; final focused Node run passed `66/66` | dirty worktree; uncommitted |
| `3eb38615` `(1.17,1.18]` | Cleans failed/invalid clones and tightens the third-party resource route | Implemented/adapted in dirty `src/endpoints/extensions.js:getManifest()`/install cleanup and `resolveExtensionResource()`/`createExtensionResourceRouteHandler()`, wired by `src/users.js` with a fixed pre-auth static bypass | Invalid/non-object manifests and clone failures remove the incomplete target; `extensions.enabled` is enforced before resource handling; encoded and normalized third-party paths bypass public static handling and reach authenticated local-first resolution with no file-level global fallback, traversal, or symlinks | Server isolation covers pre-auth bypass, gate disablement, ownership, headers, HEAD, malformed encoding, traversal, symlink, and no-fallback behavior; final focused Node run passed `66/66` | dirty worktree; uncommitted |
| `97392a4c` `(1.17,1.18]` | Refactors extension management and the assets download menu | Official object re-verified; not a manifest lifecycle dependency | Excluded from this lifecycle migration; existing cloud management UI retained | N/A; verified exclusion only, no regression claim | verified exclusion; no commit |
| `992fd8f0` `(1.15,1.16]` | Adds bulk toggling of all third-party extensions in Manage Extensions | Target already has selected bulk-toggle behavior; lifecycle work preserves it | Preserve existing cloud behavior and settings contract while lifecycle enable/disable hooks run for changed typed descriptors | Imports `34/34`, optimizations `92/92`, registration `14/14`, and invitations `10/10` passed | preserved in dirty integration; uncommitted |
| Cloud passive preload | Existing target preload uses passive `modulepreload` and stylesheet `preload` hints | Adapted in dirty `public/scripts/util/extension-resource-preload.js:preloadExtensionResources()` to consume resolver descriptors | Uses the same eligibility/loading order as activation, honors local shadowing and limits, never executes resources, cleans partial insertion, and disposes hints after activation in `loadExtensionSettings()` | Resource-preload and integration ordering cases passed in the final focused Node `66/66`; browser E2E passed `4/4` plus explicit mobile `1/1` | dirty worktree; uncommitted |
| 14 builtins | assets, attachments, caption, connection-manager, expressions, gallery, memory, quick-reply, regex, stable-diffusion, token-counter, translate, tts, vectors | All 14 dirty entrypoints export idempotent `init`; all 14 manifests declare `{ "activate": "init" }` | Gate-disabled path preserves legacy startup; gate-enabled path activates through one lifecycle import/hook and keeps one-time DOM/event/slash/worker/network setup inside the memoized initializer | Both builtin batches cover all 14 manifests, guarded startup, one-time initialization, rejection memoization, and registration placement; final focused Node `66/66`, browser E2E `4/4`, and explicit mobile `1/1` passed | dirty worktree; uncommitted |

## Swipe Picker

| Official commit / verified boundary | Official actual diff and behavior | Target state / conflict | Adaptation | Tests | Commit/push |
|---|---|---|---|---|---|
| `bf91d9af` `(1.15,1.16]` | Extracts long-press helper for World Info; not Picker UI | Helper missing | Implement once for Picker and World Info | Pointer cancel/long press | planned |
| `b04c9744` `(1.15,1.16]` | Picker UI/read/select/delete/branch; branches from `chat.slice(0,mesId+1)` | Picker absent; target chat is paged suffix | Port UI only; server is source of branch history | Read/jump/delete first | planned |
| `63feac94` `(1.16,1.17]` | Expands Picker with collapse/expand, copy, full-text rendering, popup sizing/CSS, and label refinement | Absent | Include the complete final inspection UI, not only the label | Expanded/collapsed/copy/full-text display tests | planned |
| `d2b2b1b4` `(1.17,1.18]` | Long-press opening and visual progress | Absent | Pointer-safe mobile long press | Mobile E2E | planned |
| Related swipe/greeting/command commits | Actual inspected changes are ordinary swipe/greeting/command behavior, not transactional branching | Existing behavior varies | Merge only proven prerequisites during integration | Regression tests | planned |
| Solo `/get-range` | Target returns header/messages/cursor/hasMore/revision | Missing messageOffset/total; legacy cursor is byte offset | Add absolute offset/total; server revalidates index | Legacy/chunked range matrix | planned |
| Group `/get-range` | Target already returns messageOffset/total/revision | Embedded-header offsets differ | Preserve opaque cursor and server index mapping | Embedded header tests | planned |
| Server branch API | No official server equivalent | Current solo branch saves local slice; group full-load workaround | Locked/revisioned source read, validate swipe, atomically write destination, update source only after success | Solo/group, legacy/chunked, tail-only, cross-shard, 500 swipes, fault injection | planned |

## World Info Rename Relink

| Official commit / verified boundary | Official actual diff and behavior | Target state / conflict | Adaptation | Tests | Commit/push |
|---|---|---|---|---|---|
| `bf91d9af` `(1.15,1.16]` | Common long-press and World Info UI refactor | Missing | Share helper with Picker | Desktop/mobile controls | planned |
| `a794a378` `(1.17,1.18]` | Main/auxiliary character links and `toShallow()` world; client prompts/saves | Target only updates `charLore.extraBooks`; shallow main world missing | Add shallow field, but use server plan/transaction | Shallow/full cards | planned |
| `d5b6b792` `>1.18 staging` | Current chat metadata relink and one UI test | Missing; does not scan history | Scan all solo/group headers server-side | Current/history tests | planned |
| `44af6bc1` `>1.18 staging` | Persona references and one UI test | Missing | Include all personas in one settings transaction | Current/other persona | planned |
| Official rename order | Save new -> delete old -> ask/update references | Can leave dangling links; no rollback | Plan/journal/commit/status/rollback; keep old until verification | Reject/failure/crash/rollback | planned |
| Chunked header update | Target reads `.metadata.json` before main header; revision sidecar exists | Specs omitted sidecar/revision | Update main+metadata+revision only; preserve index/shards byte hashes | Full hash invariant | planned |
| Legacy header update | First JSONL line plus possible metadata/revision | Rewriting full file can alter message bytes | Atomic first-line replacement preserving remaining bytes | Byte-for-byte message lines | planned |
| Cache/quota/isolation | settings/character/recent/chat-info caches and per-user roots | Current rename relies on separate route effects | Explicit per-handle invalidation; journal outside user quota; bounded PNG writes | A/B users, quota, cache tests | planned |

## Baseline Test Record

| Command | Runtime | Result |
|---|---|---|
| `npm run test:phase0` | Node 22.13.1 | 34 passed, 0 failed, 0 skipped |
| `npm run test:optimizations` after phase 0 | Node 22.13.1 | 91 passed, 0 failed, 0 skipped |
| `npm run test:imports` after phase 0 | Node 22.13.1 | 34 passed, 0 failed, 0 skipped |
| Focused ESLint on changed phase-0 server files | Node 22.13.1, ESLint 8.57.1 | 0 errors, 0 warnings |
| Focused ESLint on changed phase-0 and chat fixture test files | Node 22.13.1, ESLint 8.57.1 | 0 errors, 0 warnings |
| Stage 2 `tests/reasoning-tools.node.test.mjs` | Node 22.13.1 | 19 passed, 0 failed, 0 skipped |
| Stage 2 `tests/stage2-protocol-integration.node.test.mjs` | Node 22.13.1 | 7 passed, 0 failed, 0 skipped; actual CUSTOM normal/stream schema, SSE/errors/abort, Claude 4.7 multipart, premature upstream close |
| Stage 2 `ReasoningTools.e2e.js` | Playwright 1.56.1, isolated Node 22 server | 7 passed, 0 failed, 0 skipped; explicit 390x844 mobile rerun 1 passed |
| Focused Stage 2 production/test ESLint | Node 22.13.1, ESLint 8.57.1 | 0 errors, 0 warnings (`--max-warnings=0` for tests) |
| `test:user-invitations` with isolated enabled config | Node 22.13.1 | 10 passed, 0 failed, 0 skipped |
| Stage 2 canonical chat/group manifest | SHA-256 | pre/post digest `a5cbbf98118fad9daab66102fa191e5716007227285f52acf778759d02197643` (unchanged) |
| `npm run lint -- --no-cache` | ESLint 8.57.1 | Earlier phase-0 run found 339 pre-existing errors in unrelated files; not rerun for this review because requested focused lint is clean |
| `npm run test:optimizations` | Node 24.11.1 | 91 passed, 0 failed, 0 skipped |
| `npm run test:imports` | Node 24.11.1 | 34 passed, 0 failed |
| `npm run test:user-invitations` | Node 24.11.1 | 3 passed, 0 failed, 7 skipped because invitation codes are disabled in test config |
| `npm run test:registration` | Node 24.11.1 | Process-level failure after 11 assertions; Node runtime exited abnormally |
| Direct registration test | Node 22.13.1 | 14 passed, 0 failed |
| `tests npm run test:unit -- --runInBand` | Node 24.11.1 | 5 passed, 1 failed: browser-only `sample.test.js` included by Jest and `page` is undefined |
| Existing macro Playwright files against hard-coded port 8000 | Playwright 1.56.1 | 87 passed, 84 failed; failures include shared-login timeouts and assertions hitting an older server process. Not an isolated valid conformance result. |
| Isolated server startup | Node 22.13.1, port 8011 | HTTP 200; webpack compile successful |

Node 24.11.1 also exits with Windows status `3221226505` while importing/running `post-install.js`; Node 22.13.1 completes the same script. Migration validation therefore uses Node 22 unless a test explicitly checks a newer runtime.

## Verified Documentation Corrections

1. The source document baselines `d3f17c2`; fetch-time `origin/main` is `320795b4fc88c2faf410b7f1883eb0e7b686910c`.
2. The listed commit tables are dependency sets, not linear cherry-pick chains.
3. Swipe Picker core `b04c9744` is in the 1.16 boundary; `63feac94` adds expand/collapse, copy, full-text rendering, popup sizing/CSS, and a label refinement rather than only changing one label.
4. `bf91d9af` is a World Info/long-press refactor, not the Picker core.
5. `355598bb` disables unfinished output filters; it does not implement a selective pipe parser fix.
6. `2463e839` is source whitespace only, not output whitespace behavior.
7. Macro static test counts are 172 at target baseline and 512 at official 1.18 for the named files, not 171/511; staging fixes raise the set further.
8. `forwardFetchResponse` has no verified target-side 401-only clone optimization; the document's claim is not present in source.
9. Official lifecycle commits add no lifecycle automation; the required hook tests must be authored here.
10. The target chat transaction includes `.revision.json`, omitted from the source documents.
11. At baseline, runtime chat snapshots recover thrown errors but not process crashes; phase 0 adds durable pre-access recovery.
12. World Info chat/persona relink commits are post-1.18 staging, cover only current chat/personas, and remain non-transactional.
